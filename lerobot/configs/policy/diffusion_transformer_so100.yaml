# @package _global_

# Defaults for training for the PushT dataset as per https://github.com/real-stanford/diffusion_policy.
# Note: We do not track EMA model weights as we discovered it does not improve the results. See
#       https://github.com/huggingface/lerobot/pull/134 for more details.

seed: 100000
dataset_repo_id: vaishanthr/toy_pickplace

override_dataset_stats:
  # TODO(rcadene, alexander-soare): should we remove image stats as well? do we use a pretrained vision model?
  observation.images.webcam:
    # stats from imagenet, since we use a pretrained vision model
    mean: [[[0.485]], [[0.456]], [[0.406]]]  # (c,1,1)
    std: [[[0.229]], [[0.224]], [[0.225]]]  # (c,1,1)
  observation.images.gipper_cam:
    # stats from imagenet, since we use a pretrained vision model
    mean: [[[0.485]], [[0.456]], [[0.406]]]  # (c,1,1)
    std: [[[0.229]], [[0.224]], [[0.225]]]  # (c,1,1)
  
  observation.state:
    min: [-64.599609375, 13.271484375, -0.52734375, 22.763671875, -89.6484375, -3.1854379177093506]
    max: [69.9609375, 188.7890625, 174.90234375, 102.919921875, 93.69140625, 72.69624328613281]
  action:
    min: [-65.0390625, 16.875, -13.271484375, 21.181640625, -121.2890625, -6.7237162590026855]
    max: [70.224609375, 193.095703125, 174.7265625, 109.599609375, 94.306640625, 73.83863067626953]


training:
  offline_steps: 50000
  online_steps: 0
  eval_freq: -1
  save_freq: 10000
  save_checkpoint: true

  batch_size: 8
  grad_clip_norm: 10
  lr: 1.0e-4
  lr_scheduler: cosine
  lr_warmup_steps: 1000
  adam_betas: [0.95, 0.999]
  adam_eps: 1.0e-8
  adam_weight_decay: 1.0e-6
  online_steps_between_rollouts: 1

  transformer_weight_decay: 1e-3

  delta_timestamps:
    observation.images.webcam: "[i / ${fps} for i in range(1 - ${policy.n_obs_steps}, 1)]"
    observation.images.gipper_cam: "[i / ${fps} for i in range(1 - ${policy.n_obs_steps}, 1)]"
    observation.state: "[i / ${fps} for i in range(1 - ${policy.n_obs_steps}, 1)]"
    action: "[i / ${fps} for i in range(1 - ${policy.n_obs_steps}, 1 - ${policy.n_obs_steps} + ${policy.horizon})]"

  # The original implementation doesn't sample frames for the last 7 steps,
  # which avoids excessive padding and leads to improved training results.
  drop_n_last_frames: 7  # ${policy.horizon} - ${policy.n_action_steps} - ${policy.n_obs_steps} + 1

eval:
  n_episodes: 50
  batch_size: 50

policy:
  name: diffusion

  # Input / output structure.
  n_obs_steps: 4
  horizon: 100
  n_action_steps: 100

  input_shapes:
    # TODO(rcadene, alexander-soare): add variables for height and width from the dataset/env?
    observation.images.webcam: [3, 480, 640]
    observation.images.gipper_cam: [3, 480, 640]
    observation.state: ["${env.state_dim}"]
  output_shapes:
    action: ["${env.action_dim}"]

  # Normalization / Unnormalization
  input_normalization_modes:
    observation.images.webcam: mean_std
    observation.images.gipper_cam: mean_std
    observation.state: min_max
  output_normalization_modes:
    action: min_max

  # Architecture / modeling.
  # Vision backbone.
  vision_backbone: resnet18
  crop_shape: null
  crop_is_random: False
  pretrained_backbone_weights: null
  use_group_norm: True
  spatial_softmax_num_keypoints: 32
  # Transformer
  use_transformer: True
  n_layer: 8
  n_head: 4
  p_drop_emb: 0.0
  p_drop_attn: 0.3
  causal_attn: True
  n_cond_layers: 8
  diffusion_step_embed_dim: 256
  # Noise scheduler.
  noise_scheduler_type: DDPM
  num_train_timesteps: 10
  beta_schedule: squaredcos_cap_v2
  beta_start: 0.0001
  beta_end: 0.02
  prediction_type: epsilon # epsilon / sample
  clip_sample: True
  clip_sample_range: 1.0

  # Inference
  num_inference_steps: 10  # if not provided, defaults to `num_train_timesteps`

  # Loss computation
  do_mask_loss_for_padding: false
